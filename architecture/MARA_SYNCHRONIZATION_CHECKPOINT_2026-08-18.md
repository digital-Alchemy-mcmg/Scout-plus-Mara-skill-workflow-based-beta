# MARA / MARA++ — Synchronization Checkpoint 2026-08-18

**Authority state:** CANONICAL SYNCHRONIZATION CHECKPOINT  
**Supersedes for newly admitted mechanisms:** `MARA_SYNCHRONIZATION_FREEZE_2026-08-17.md`  
**Repository:** `digital-Alchemy-mcmg/Scout-plus-Mara-skill-workflow-based-beta`

## 1. Purpose

This checkpoint performs the explicit synchronization required by the 2026-08-17 freeze after the résumé test exposed two genuine missing requirements: recent chronology accountability/User Override recovery and deterministic résumé-surface selection.

The three-container architecture remains unchanged:

**SCOUT → MARA → Artifact Model**

No new evidence domain is added. Candidate Core remains immutable and outside the five evidence domains.

## 2. Newly Admitted MARA Mechanisms

### 2.1 Six-Year Chronology Accountability

Before Candidate Freeze, MARA must attempt to establish six years of recent temporal accountability using Work History as the primary chronology-bearing domain plus legitimate cross-domain temporal anchors.

The target is accountability, not uninterrupted employment.

Declared gaps are valid chronology states. Temporal anchors may not exceed their supported duration. Unknown material gaps may survive freeze and later trigger a mandatory-artifact halt.

Governing document:

`architecture/MARA_SIX_YEAR_CHRONOLOGY_ACCOUNTABILITY_2026-08-18.md`

### 2.2 User Override

When MARA reaches an unresolved mandatory artifact requirement it cannot legally resolve from admitted evidence, it halts and may accept explicit user operational authority under `USER_OVERRIDE`.

`USER_OVERRIDE` is operational, not evidentiary. It never retroactively strengthens evidence or becomes independent corroboration.

Override events and downstream dependency must be measured on the canonical pre-render artifact representation. Literal user-supplied lines and override propagation are separate metrics. Missing telemetry is never fabricated.

Governing document:

`architecture/MARA_USER_OVERRIDE_PROVENANCE_PERFORMANCE_2026-08-18.md`

Schema:

`schemas/SCHEMA_USER_OVERRIDE.md`

## 3. Newly Admitted Artifact-Model Mechanism

### 3.1 Résumé Surface Registry

Résumé geometry is now governed as a retained surface library rather than regenerated ad hoc.

The fixed logical artifact payload is measured first. The selector then filters retained surfaces by density, comfortable character capacity, topology, PDF fitness, wrap slack, and destination requirements.

Candidate truth is never rewritten to rescue a surface.

If multiple surfaces remain materially equivalent after deterministic filtering, the user may choose among those already-valid surfaces. Such a surface choice does not alter candidate evidence or provenance accounting.

Governing document:

`architecture/MARA_RESUME_SURFACE_REGISTRY_2026-08-18.md`

Schema:

`schemas/SCHEMA_RESUME_SURFACE_REGISTRY.md`

## 4. Validation Receipt Admitted

The UO-001 résumé chronology test is retained as implementation/behavioral evidence:

`validation/MARA_UO001_RESUME_TEST_RECEIPT_2026-08-18.md`

The corrected test state is:

`FROZEN — USER_OVERRIDE UO-001 ATTACHED — ACCOUNTABILITY SPLIT CORRECTED`

Key fixed payload facts:

- `F = 41` countable canonical lines;
- 2,342 characters including line breaks / 2,301 excluding;
- four employment records;
- two separate overlapping 2023–Present accountability tracks:
  - OCC — Data Analytics / HCI;
  - Metro Concepts / Relax;
- six total chronology/accountability objects;
- chronology depth 12/2012–Present;
- prior 40-line merged state superseded.

Halt baseline and propagation delta were unavailable and remain unavailable; the test correctly did not fabricate them.

## 5. Pipeline Consequences

### MARA

MARA now owns:

- upstream chronology-accountability attempt before freeze;
- preservation of unknown versus declared gaps;
- legal halt at unresolved mandatory artifact requirements;
- User Override event/provenance accounting;
- bounded use of operational authority downstream.

### Artifact Model

Artifact Model now owns:

- fixed-payload surface selection from the retained registry;
- geometry validation against real content volume/shape;
- valid human surface choice only after deterministic filtering;
- final rendering without modification of candidate truth.

## 6. Current Résumé Test Completion State

For the active résumé test, epistemic/candidate-truth work is complete.

The fixed payload is ready for surface reconciliation.

The remaining test path is:

**surface selection → render → delivery validation**

No new candidate evidence, chronology reconstruction, or authority modification is authorized during surface selection/rendering.

## 7. Relationship to 2026-08-17 Freeze

All non-conflicting architecture from the 2026-08-17 checkpoint remains controlling.

This checkpoint specifically admits and governs the mechanisms that the prior freeze required to be surfaced for explicit synchronization rather than silently added during implementation.

PR #1 remains isolated and unresolved. This synchronization is based on canonical `main` and does not merge or modify PR #1.

## 8. Governing Invariants

- Candidate Core is not an evidence domain.
- Five evidence domains remain unchanged.
- Absence is not negative evidence.
- Declared gap is not adverse evidence.
- Candidate truth is constructed before destination projection.
- Freeze closes normal evidence admission.
- User Override is operational authority, not evidentiary authority.
- Override telemetry cannot be altered by render geometry.
- A surface selector cannot rewrite evidence to make geometry fit.
- Artifact Model remains downstream and cannot independently discover candidate truth.
- Rendering is the next authorized operation for the corrected résumé test after valid surface selection.
